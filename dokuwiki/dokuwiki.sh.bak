#!/bin/bash

#If you haven't done this before, create a new network for the application:

#docker network create dokuwiki-tier


#Run the dokuwiki in the contatiner:

docker run -d -p 10005:80 -p 443:443 --name dokuwiki -e DOKUWIKI_USERNAME=dokuwiki -e DOKUWIKI_PASSWORD=dokuwiki -e DOKUWIKI_FULL_NAME=superuser -e DOKUWIKI_EMAIL=mygithublab@126.com -e DOKUWIKI_WIKI_NAME=CDC DokuWiki \
  --net dokuwiki-tier \
  --volume /path/to/dokuwiki-persistence:/bitnami/dokuwiki \
  --restart=always \
  bitnami/dokuwiki


#Take a snapshot of the appliction state

#rsync -a /path/to/dokuwiki-persistence /path/to/dokuwiki-persistence.bkp.$(date +%Y%m%d-%H.%M.%S)


